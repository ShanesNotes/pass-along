# Supabase Migrations

Migrations are additive-only: create new tables, add new columns, add indexes, or add policies in new migration files. Never drop tables or columns, rename objects, or alter a column type in place; ship a new compatible shape and migrate readers forward instead.
