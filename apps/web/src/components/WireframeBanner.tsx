export function WireframeBanner() {
  return (
    <div
      style={{
        background: "#3a3a3a",
        color: "#f5f5f5",
        padding: "6px 16px",
        fontSize: "0.8rem",
        letterSpacing: "0.04em",
        textAlign: "center"
      }}
    >
      WIREFRAME — synthetic data, no live model calls, nothing is stored
    </div>
  );
}
